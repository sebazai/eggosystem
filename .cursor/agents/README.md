## Cursor subagents

This folder defines specialized subagents used by the main coding agent.

Each agent file includes:

- **Name**
- **Description**
- **Model**: `fast` (throughput) or `smart` (reasoning/architecture)
- **Must-read rules**: rule files to read before starting work
- **Operating constraints**: guardrails the agent must follow
