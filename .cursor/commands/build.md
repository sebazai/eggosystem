---
name: build
description: Build all packages and apps
---

Build all packages and applications in the monorepo.

This runs `pnpm build` which uses Turbo to build all workspaces in the correct dependency order.
