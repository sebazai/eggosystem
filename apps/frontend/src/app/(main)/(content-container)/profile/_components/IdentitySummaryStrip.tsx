// Bold-variant only — rendered behind flags.profile.bold (default false).
// This component intentionally returns null; it is a placeholder for Variant B.
export function IdentitySummaryStrip() {
  return null;
}
