---
name: lint-fix
description: Run ESLint with auto-fix
---

Run ESLint across the project and automatically fix any fixable issues.

This runs `pnpm lint:fix` which uses Turbo to run ESLint with the `--fix` flag across all workspaces.
